# name: test/sql/scalar_functions.test
# description: Test sql_smart_auto_complete
# group: [sql]

require smart_autocomplete

query II
SELECT suggestion, suggestion_start FROM sql_smart_auto_complete('select gam') LIMIT 1;
----
gamma	7

query II
SELECT suggestion, suggestion_start FROM sql_smart_auto_complete('select nexta') LIMIT 1;
----
nextafter	7

query II
SELECT suggestion, suggestion_start FROM sql_smart_auto_complete('select bit_l') LIMIT 1;
----
bit_length	7
