# name: test/sql/suggest_file.test
# description: Test sql_smart_auto_complete
# group: [sql]

require notwindows

require smart_autocomplete

# test file auto complete
query I
SELECT suggestion FROM sql_smart_auto_complete('COPY tbl FROM ''{DATA_DIR}/parq') LIMIT 1;
----
parquet-testing/

query I
SELECT suggestion FROM sql_smart_auto_complete('COPY tbl FROM ''{DATA_DIR}/csv/all_quote') LIMIT 1;
----
all_quotes.csv'
